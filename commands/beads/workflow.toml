description = "Show the AI-supervised issue workflow guide"
prompt = """
Display the beads workflow for AI agents and developers.

Usage:
- Show workflow: `cat .beads/BD_GUIDE.md` (if available) or summarize standard workflow.

Use `run_shell_command` to execute `bd help workflow` or simply explain the workflow.
"""
